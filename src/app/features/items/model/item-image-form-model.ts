export interface ItemImageForm {
    tempId: string;
    file: File;
    order: number;
}

export interface ItemImageView {
    tempId: string;
    file: File;
    previewUrl: string;
    order: number;
}